{
  "name": "engrid-scripts",
  "private": true,
  "devDependencies": {
    "@babel/preset-env": "^7.12.13",
    "css-loader": "^5.1.1",
    "lerna": "^3.22.1",
    "prettier": "2.4.1",
    "style-loader": "^2.0.0",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "lerna bootstrap & lerna run build",
    "version": "node ./write-version.js && npm run build",
    "make": "lerna run build && lerna publish --yes --cd-version patch",
    "make-feature": "lerna run build && lerna publish --yes --cd-version minor && npm run write-version && git add . && git commit -m Version-bump && git push "
  },
  "dependencies": {
    "sanitize.css": "^12.0.1",
    "tippy.js": "^6.3.1"
  }
}
